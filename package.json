{
  "name": "massifyx-intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "MassifyX Intelligence Service (MIS) — supply chain disruption ingest, enrichment, and read API",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js",
    "test": "node --test test/*.test.js",
    "eval": "node scripts/run-eval.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "adm-zip": "^0.6.0",
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.1",
    "helmet": "^8.3.0",
    "pg": "^8.22.0",
    "ws": "^8.21.1"
  }
}
